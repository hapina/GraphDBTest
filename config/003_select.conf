[DEFAULT]
# Type of experiment
experiment_type=select
# The count of iteration (1-100)
iteration=10
# Database name
db_name=db1
# Sequence of queries
commands=g.V().count()
    |g.E().count()


#g.V('name','first').out.loop(1){it.object.name!="fifth" && it.loops<6}.path.filter{it.last().name=="fifth"}.transform{it.name}.groupBy{it.size()}{it}.cap.next()

    
