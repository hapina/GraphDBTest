[DEFAULT]
# Type of experiment
experiment_type=delete
# Database name
db_name=testdb001
# Sequence of queries
commands=g.removeVertex(g.V('name','second').next())
    |g.removeVertex(g.V('name','fourth').next())
    |g.removeVertex(g.V('name','fifth').next())
