[DEFAULT]
# Type of experiment
experiment_type=delete
# Database name
db_name=db1
# Sequence of queries
commands=g.V().has(label,'second').drop()
    |g.V().has(label,'fourth').drop()
    |g.V().has(label,'fifth').drop()
