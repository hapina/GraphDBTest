[DEFAULT]
# Type of experiment
experiment_type=delete
# Type of logging default|file|none
logging=default
# Monitoring yes|no
monitoring=yes
# Database name
db_name=testdb001
# Sequence of queries
commands=g.removeVertex(g.v(2))
    |g.removeVertex(g.v(4))
    |g.removeVertex(g.v(5))
