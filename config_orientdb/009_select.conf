[DEFAULT]
# Type of experiment
experiment_type=select
# The count of iteration (1-100)
iteration=1
# Database name
db_name=testdb002
# Sequence of queries
commands=g.V().count()
    |g.E().count()

    
