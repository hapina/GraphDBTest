[DEFAULT]
# name of experiment
name=select
# the number of repetitions (1-100)
repetition=1
# place of logging db|file|none
logging=db
# Sequence of queries
#command= select * from E
command= select * from V
